{
  "name": "social-post-pipeline-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm run preview",
    "dev:api": "wrangler dev --local --live-reload",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "build:styles": "pnpm exec tailwindcss -i ./src/styles/template.css -o ./src/generated/template.css",
    "build:assets": "pnpm run build:styles && node ./scripts/embed-template-assets.mjs",
    "watch:assets": "pnpm exec chokidar \"templates/**/*.html\" \"src/styles/**/*.css\" \"config/pipeline/**/*.yaml\" \"scripts/embed-template-assets.mjs\" -c \"pnpm run build:assets\" --debounce 250",
    "preview": "pnpm run build:assets && pnpm exec concurrently --kill-others-on-fail --names \"assets,worker\" --prefix-colors \"yellow,cyan\" \"pnpm run watch:assets\" \"pnpm exec wrangler dev --local --live-reload --ip 127.0.0.1 --port 8787 --var API_AUTH_REQUIRE_FOR_PREVIEW:false\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^9.2.1",
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0",
    "typescript": "latest",
    "vitest": "^3.2.4",
    "wrangler": "latest",
    "yaml": "^2.8.1"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "1.0.6"
  }
}
